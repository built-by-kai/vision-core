// ─── generate.js — diagnostic stub ─────────────────────────────────────────
export default async function handler(req, res) {
  return res.json({ status: "generate route alive", method: req.method, query: req.query })
}
